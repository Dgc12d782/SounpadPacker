# SounpadPacker
tiny util that takes an .spl soundpad file, fetches all the files from it and copies them into a given directory, then copies the given .spl into that directory and patches the paths in it
